# Prettier is here to fill gaps this repo has no other formatter for —
# today that means Markdown and YAML. Everything below is either somebody
# else's file or a format Prettier has no business reformatting.
node_modules/
.bun-cache/
bun.lock

# Built RPM artefacts.
*.rpm
*.src.rpm

# Written by the release job, not by hand.
CHANGELOG.md

# `openspec init`'s generated skill/command docs — somebody else's docs,
# held to somebody else's rules.
.claude/
